<% if flash[:errors] && !flash[:errors].empty? %>
  <div class="jumbotron text-center alert alert-danger">
    <h1>(╯°□°）╯︵ ┻━┻)</h1>
    <p>Something went wrong...</p>
    <h2 class="error-text"><%= flash[:errors] %></h2>
  </div>
<% end %>
